#include "monty.h"
/**
 * stack_op - it prints the top
 * @head: a pointer to the head of the stack
 * @counter: the current line number
 * Return: nothing
*/

void stack_op(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
